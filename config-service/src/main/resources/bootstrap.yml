server:
  port: 8888

info:
  name: "Config Service"

spring:
  application:
    name: config-service
#  profiles:
#    active: native  # For local file repository only
  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://github.com/rajeshjaava/netflix-oss-example-config-repo
